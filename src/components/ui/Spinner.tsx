import { AtomSpinner } from 'react-epic-spinners';
const Spinner = () => {
	return (
		<div className='spinner__conteiner'>
			<AtomSpinner color='#ff686b' />;
		</div>
	);
};

export default Spinner;
